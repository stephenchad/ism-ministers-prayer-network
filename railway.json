{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "PHP",
    "phpConfig": {
      "version": "8.1"
    }
  },
  "deploy": {
    "startCommand": "php artisan serve --host 0.0.0.0 --port $PORT",
    "restartPolicy": {
      "type": "ON_FAILURE"
    }
  }
}
